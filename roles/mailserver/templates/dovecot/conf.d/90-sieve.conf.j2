plugin {
    sieve = {{ mailserver_vmail_dir }}/%d/%n/.dovecot.sieve
    sieve_default = {{ mailserver_vmail_home }}/sieve/default.sieve
    sieve_dir = {{ mailserver_vmail_dir }}/%d/%n/sieve
    sieve_global_dir = {{ mailserver_vmail_home }}/sieve
}
